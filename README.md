# YAL's Foundry Demigrator

So you exported your character JSON from Foundry and it doesn't import into another instance because yours is 0.001 versions newer.

This little tool fixes that by simply changing all the version numbers to not be newer than that. For minor version changes, this is just fine.